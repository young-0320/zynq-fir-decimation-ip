# 하드웨어 파이프라인

## 시나리오 0 — 비교 시연 (FIR 없을 때 vs 있을 때)

```
[PC: Python]
  동일한 멀티톤 신호에 대해 두 결과를 나란히 표시

  (좌) 다운샘플링만 적용 (FIR 없음)
       → 앨리어싱 발생: 25MHz 이상 톤이 저주파에 유령 피크로 나타남

  (우) FIR + 데시메이터 적용 (우리 시스템)
       → 유령 피크 사라짐
```

PC Python의 `downsample_only_ideal` 모델로 계산하므로 보드 불필요. "왜 FIR이 필요한가"를 한 번에 설명.

## 시나리오 1 — 기본 동작 시연

파이프라인 구조는 1-1과 1-2 공통:

```
[PS: ARM Cortex-A9 / bare-metal C]
  C 코드로 멀티톤 신호 미리 생성 → DDR 메모리 저장
        ↓ AXI DMA (DDR → PL, 100MHz)
[PL: FPGA — Verilog RTL]
  s_axis: AXI-Stream 입력
  FIR 필터 (N=43) + M=2 데시메이터
  m_axis: AXI-Stream 출력
        ↓ AXI DMA (PL → DDR, 50MHz)
[PS: ARM Cortex-A9 / bare-metal C]
  결과 버퍼 수신 → UART로 PC에 전송
        ↓ USB 시리얼 (UART)
[PC: Python]
  FFT 계산 → 입출력 파형 화면에 표시
```

### 시나리오 1-1 — Happy case

| 항목 | 값 |
|---|---|
| 샘플 수 | 8192 |
| 톤 구성 | 5MHz / 20MHz / 30MHz |
| 각 톤 진폭 | 0.3 (균등) |
| 합산 최대 진폭 | 0.9 |

| 톤 | 필터 영역 | 예상 결과 |
|---|---|---|
| 5MHz | 통과대역 | FFT에서 살아있음 |
| 20MHz | 전이대역 | 부분 감쇠 |
| 30MHz | 저지대역 | FFT에서 사라짐 |

필터가 통과/차단을 명확히 보여주는 기본 케이스.

### 시나리오 1-2 — Edge case

| 항목 | 값 |
|---|---|
| 샘플 수 | 8192 |
| 톤 구성 | 7MHz / 15MHz / 25MHz / 45MHz |
| 각 톤 진폭 | 0.225 (= 0.9 / 4, 균등) |
| 합산 최대 진폭 | 0.9 |

| 톤 | 필터 영역 | 예상 결과 |
|---|---|---|
| 7MHz | 통과대역 | FFT에서 살아있음 |
| 15MHz | fp 경계값 | 거의 통과 (리플 범위 내) |
| 25MHz | fs 경계값 | ≥ 60dB 감쇠 (계수 양자화로 ±수dB 오차 가능) |
| 45MHz | 저지대역 깊숙이 | FFT에서 사라짐 |

필터 스펙 경계(fp, fs)에서 실제 동작을 확인하는 케이스. 계수 양자화 효과도 함께 관찰 가능.

## 시나리오 2 — 인터랙티브 시연

```
[청중]
  원하는 주파수 값 제시 (예: "10MHz, 40MHz")
        ↓ 키보드 입력
[PC: Python]
  주파수 값으로 멀티톤 입력 파형 즉시 계산 → 화면에 표시
  주파수 값(숫자 몇 바이트)을 UART로 전송
        ↓ USB 시리얼 (UART)
[PS: ARM Cortex-A9 / bare-metal C]
  주파수 값 수신 → 멀티톤 신호 즉석 생성 → DDR 메모리 저장
        ↓ AXI DMA (DDR → PL, 100MHz)
[PL: FPGA — Verilog RTL]
  s_axis: AXI-Stream 입력
  FIR 필터 (N=43) + M=2 데시메이터
  m_axis: AXI-Stream 출력
        ↓ AXI DMA (PL → DDR, 50MHz)
[PS: ARM Cortex-A9 / bare-metal C]
  결과 버퍼 수신 → UART로 PC에 전송
        ↓ USB 시리얼 (UART)
[PC: Python]
  FFT 계산 → 출력 파형 화면에 표시
```

## 역할 분담

| 위치    | 언어           | 역할                                         |
| ------- | -------------- | -------------------------------------------- |
| PC      | Python         | 키보드 입력, UART 통신, 입출력 파형/FFT 표시 |
| 보드 PS | C (bare-metal) | 신호 생성, DMA 제어, UART 송수신             |
| 보드 PL | Verilog        | FIR 필터 + 데시메이터 실제 연산              |

## 처리 방식

블록 처리 방식. 체감 지연 수백ms 이내.

시나리오 0은 PC만으로 앨리어싱 문제를 시각적으로 보여주며 시스템의 필요성을 설명.
시나리오 1-1은 5/20/30MHz 고정 신호로 통과/차단 기본 동작을 보여줌.
시나리오 1-2는 7/15/25/45MHz로 fp·fs 경계값 동작과 양자화 효과를 보여줌.
시나리오 2는 청중이 직접 주파수를 지정해서 필터 특성을 체험. UART로 주파수 값(숫자 몇 바이트)만 전송하므로 속도 문제 없음.

## 데모 포인트

fp=15MHz, fs=25MHz 필터 스펙을 눈으로 확인.
- 15MHz 이하 톤 → FFT에서 살아있음
- 25MHz 이상 톤 → FFT에서 사라짐

![하드웨어 파이프라인](./asset/PSPLDMAAXI.png)
